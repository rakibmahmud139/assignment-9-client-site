import { Box } from "@mui/material";
import AdminDetails from "../components/AdminDetails";

const page = () => {
  return (
    <Box mb={36}>
      <AdminDetails />
    </Box>
  );
};

export default page;
